/*
import {HistogramPipe} from './histogram.pipe';

fdescribe('HistPipe', () => {
  it('create an instance', () => {
    const pipe = new HistogramPipe();
    expect(pipe).toBeTruthy();
  });

  it('should display in grouped format', () => {
    const dataset = ;
    const xlabel = '';
    const zlable = '';

    const pipe = new HistogramPipe();

    const result = pipe.transform(dataset, xlabel, zlable);

    //expect(result).toBe();

  });
});

*/
